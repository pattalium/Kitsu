#pragma once

#include <Arduino.h>

constexpr uint8_t KITSU_SPRITE_WIDTH = 64;
constexpr uint8_t KITSU_SPRITE_HEIGHT = 64;
constexpr uint8_t KITSU_SPRITE_FRAMES = 8;
constexpr size_t KITSU_SPRITE_BYTES = 512;

// Companion artwork is intentionally supplied only by installed .k868 packs.
